version: '3.9'
services:
  backend:
    build:
      context: ./backend
    ports:
      - '4000:4000'

  frontend:
    build:
      context: ./frontend
    depends_on:
      - backend
    environment:
      NEXT_PUBLIC_API_BASE: http://localhost:4000/api
    ports:
      - '3000:3000'
